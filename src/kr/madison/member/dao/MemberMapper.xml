<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
<insert id="addNewMember" parameterType="MemberVO">
	INSERT INTO info_userinfo(
		userid,
		passwd,
		korname,
		nickname,
		email,
		telnum,
		gender,
		birthdate,
		major,
		minor,
		regdate,
		moddate,
		authid
	) VALUES(
		#{userid},
		#{passwd},
		#{korname},
		#{nickname},
		#{email},
		#{telnum},
		#{gender},
		#{birthdate},
		#{major},
		#{minor},
		NOW(),
		NOW(),
		#{authid}
	)
</insert>
<select id="chkId" parameterType="MemberVO" resultType="MemberVO">
	SELECT
		userno
	FROM info_userinfo
	WHERE userid = #{userid}
</select>
<select id="chkNickname" parameterType="MemberVO" resultType="MemberVO">
	SELECT
		userno
	FROM info_userinfo
	WHERE nickname = #{nickname}
</select>
</mapper>