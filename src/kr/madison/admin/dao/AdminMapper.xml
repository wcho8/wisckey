<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Admin">
	<select id="findEmpBoardTypes" resultType="AdminVO" parameterType="AdminVO">
		SELECT
			empid as bid,
			name,
			comment,
			regdate,
			useyn
		FROM cod_employment
	</select>
	<select id="findRestBoardTypes" resultType="AdminVO" parameterType="AdminVO">
		SELECT
			tid as bid,
			name,
			comment,
			regdate,
			useyn
		FROM cod_employment
	</select>
	<select id="findSaleBoardTypes" resultType="AdminVO" parameterType="AdminVO">
	
	</select>
	<select id="findFreeBoardTypes" resultType="AdminVO" parameterType="AdminVO">
		SELECT
			frid as bid,
			name,
			comment,
			regdate,
			useyn
		FROM cod_freeboard
	</select>
	<select id="findSchoolBoardTypes" resultType="AdminVO" parameterType="AdminVO">
		SELECT
			swid as bid,
			name,
			comment,
			regdate,
			useyn
		FROM cod_schoolwork
	</select>
	<select id="findBoardTypeList" resultType="AdminVO" parameterType="AdminVO">
		SELECT
			typeid,
			name as typename,
			comment,
			regdate,
			useyn
		FROM cod_boardtypes
		<choose>
		<when test="ptypeid == null or ptypeid == ''">
			WHERE ptypeid = 0
		</when>
		<otherwise>
			WHERE ptypeid = #{ptypeid}
		</otherwise>
		</choose>
	</select>
</mapper>
